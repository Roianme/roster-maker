name: roster_maker
description: Restaurant roster maker - Admin Web App
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^2.24.0
  firebase_auth: ^4.15.0
  cloud_firestore: ^4.13.0
  
  # State Management
  provider: ^6.1.0
  
  # UI
  flutter_web_plugins:
    sdk: flutter
  google_fonts: ^6.1.0
  intl: ^0.18.0
  
  # Utilities
  csv: ^6.0.0
  http: ^1.1.0
  file_saver: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
